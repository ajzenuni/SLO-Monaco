{
  "enabled": true,
  "name": "{{ .name }}",
  "description": "",
  "metricExpression": "(100)*(({{ .calcMetric }}:splitby():count)/(builtin:apps.web.action.count.{{ .actionType }}.browser:filter(eq(\"dt.entity.application_method\",{{ .methodId}})):splitBy()))",
  "target": {{ .target }},
  "warning": {{ .warning }},
  "evaluationType": "AGGREGATE",
  "timeframe": "{{.timeFrame}}"
}