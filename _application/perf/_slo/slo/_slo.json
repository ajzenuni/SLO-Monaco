{
  "enabled": true,
  "name": "{{ .name }}",
  "description": "",
  "metricExpression": "(100)*(({{ .calcMetric }}:splitby():count)/(builtin:apps.web.actionCount.category:filter(in(\"dt.entity.application\",entitySelector(\"type(APPLICATION),entityName({{ .appName }})\"))):splitBy()))",
  "target": {{ .target }},
  "warning": {{ .warning }},
  "evaluationType": "AGGREGATE",
  "timeframe": "{{.timeFrame}}"
}