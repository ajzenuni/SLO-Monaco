{
  "enabled": true,
  "name": "{{ .name }}",
  "description": "",
  "metricExpression": "(100)*(({{ .calcMetric }}:splitBy():sum)/(builtin:service.keyRequest.count.total:filter(eq(\"dt.entity.service_method\",{{ .methodId}})):splitby()))",
  "target": {{ .target }},
  "warning": {{ .warning }},
  "evaluationType": "AGGREGATE",
  "timeframe": "{{.timeFrame}}"
}